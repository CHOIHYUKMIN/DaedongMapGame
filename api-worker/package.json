{
    "name": "daedong-api",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    },
    "dependencies": {
        "@neondatabase/serverless": "^0.9.0"
    }
}