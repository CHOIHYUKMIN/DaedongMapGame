# 🏗️ 대동맛집지도 시스템 아키텍처

**문서 버전:** v1.0.0  
**최종 업데이트:** 2025-12-28  
**담당자:** AI Assistant

---

## 📋 목차

1. [시스템 개요](#시스템-개요)
2. [아키텍처 다이어그램](#아키텍처-다이어그램)
3. [기술 스택](#기술-스택)
4. [프로젝트 구조](#프로젝트-구조)
5. [데이터 흐름](#데이터-흐름)
6. [배포 환경](#배포-환경)
7. [CI/CD 파이프라인](#cicd-파이프라인)
8. [보안 구성](#보안-구성)
9. [모니터링 및 로깅](#모니터링-및-로깅)

---

## 시스템 개요

**대동맛집지도**는 전국 지역의 동(洞) 단위 맛집을 탐험하는 퍼즐 게임입니다.

### 핵심 기능
- 🗺️ **전국 지도 탐험**: 17개 시/도, 228개 시/군/구, 140+ 동(洞) 데이터
- 🧩 **매치-3 퍼즐 게임**: 3개 이상 블록 매칭으로 목표 달성
- 🍽️ **맛집 수집**: 지역별 특색있는 맛집 정보 수집
- 📱 **PWA 지원**: 모바일 앱처럼 설치 가능
- ☁️ **클라우드 동기화**: 여러 기기에서 진행 상황 동기화

---

## 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           CLIENT (Browser/PWA)                          │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │   HTML5     │  │    CSS3     │  │ JavaScript  │  │  Leaflet.js │    │
│  │  (index)    │  │  (style)    │  │  (ES6+)     │  │   (지도)    │    │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                     JavaScript Modules                          │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │   │
│  │  │ game.js  │ │puzzle.js │ │data-     │ │dong-     │           │   │
│  │  │          │ │          │ │loader.js │ │registry  │           │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │   │
│  │  │ audio-   │ │firebase- │ │cloud-    │ │ region-  │           │   │
│  │  │manager.js│ │sync.js   │ │sync.js   │ │ data.js  │           │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTPS
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         HOSTING & API LAYER                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────┐           ┌──────────────────────┐           │
│  │   Firebase Hosting   │           │  Cloudflare Workers  │           │
│  │                      │           │      (API)           │           │
│  │  • 정적 파일 호스팅   │           │                      │           │
│  │  • SSL/TLS 인증서    │           │  • REST API 엔드포인트│           │
│  │  • CDN 캐싱          │◄─────────►│  • 인증 미들웨어      │           │
│  │  • SPA 라우팅        │           │  • 요청 유효성 검증   │           │
│  │                      │           │  • Edge Computing    │           │
│  │  URL:                │           │                      │           │
│  │  daedongmapgame      │           │  URL:                │           │
│  │  .web.app            │           │  daedong-api         │           │
│  │                      │           │  .workers.dev        │           │
│  └──────────────────────┘           └──────────────────────┘           │
│                                              │                          │
└──────────────────────────────────────────────┼──────────────────────────┘
                                               │
                                               │ Neon Serverless Driver
                                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           DATABASE LAYER                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                    Neon PostgreSQL (Serverless)                   │  │
│  │                                                                    │  │
│  │   Tables:                                                          │  │
│  │   ┌────────────┐  ┌────────────┐  ┌────────────┐                  │  │
│  │   │   users    │  │  progress  │  │  sessions  │                  │  │
│  │   │            │  │            │  │            │                  │  │
│  │   │ • user_id  │  │ • user_id  │  │ • session  │                  │  │
│  │   │ • email    │  │ • game_data│  │ • user_id  │                  │  │
│  │   │ • name     │  │ • sync_at  │  │ • expires  │                  │  │
│  │   └────────────┘  └────────────┘  └────────────┘                  │  │
│  │                                                                    │  │
│  │   Features:                                                        │  │
│  │   • Auto-scaling (Serverless)                                      │  │
│  │   • 0.5초 Cold Start                                               │  │
│  │   • 분기당 무료 티어                                               │  │
│  │                                                                    │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                                               
┌─────────────────────────────────────────────────────────────────────────┐
│                        AUTHENTICATION LAYER                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                      Firebase Authentication                       │  │
│  │                                                                    │  │
│  │   Providers:                                                       │  │
│  │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │  │
│  │   │   Google    │  │   Email     │  │  Anonymous  │               │  │
│  │   │   OAuth     │  │   /Pass     │  │   Guest     │               │  │
│  │   └─────────────┘  └─────────────┘  └─────────────┘               │  │
│  │                                                                    │  │
│  │   Features:                                                        │  │
│  │   • JWT 토큰 발급                                                  │  │
│  │   • 세션 관리                                                      │  │
│  │   • 소셜 로그인 통합                                               │  │
│  │                                                                    │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 기술 스택

### Frontend
| 기술 | 용도 | 버전 |
|------|------|------|
| **HTML5** | 마크업 | - |
| **CSS3** | 스타일링 (Vanilla CSS) | - |
| **JavaScript** | 클라이언트 로직 | ES6+ |
| **Leaflet.js** | 지도 렌더링 (OpenStreetMap) | 1.9.x |
| **Howler.js** | 오디오 관리 | 2.2.x |

### Backend / API
| 기술 | 용도 | 특징 |
|------|------|------|
| **Cloudflare Workers** | Serverless API | Edge Computing, 글로벌 배포 |
| **Hono.js** | API 프레임워크 | 경량, Cloudflare 최적화 |

### Database
| 기술 | 용도 | 특징 |
|------|------|------|
| **Neon PostgreSQL** | 사용자 데이터 저장 | Serverless, Auto-scaling |

### 데이터 저장소 상세

#### 1. Neon PostgreSQL (클라우드 데이터베이스)

> **역할**: 사용자 데이터 및 진행 상황 클라우드 동기화

| 항목 | 설명 |
|------|------|
| **서비스** | [Neon](https://neon.tech) - Serverless PostgreSQL |
| **리전** | `ap-southeast-1` (싱가포르) |
| **연결 방식** | `@neondatabase/serverless` 드라이버 |
| **엔드포인트** | `ep-autumn-dawn-a19595o7-pooler.ap-southeast-1.aws.neon.tech` |

**테이블 구조:**

```sql
-- 사용자 정보 (핵심 테이블)
users (
  id SERIAL PRIMARY KEY,
  uid VARCHAR(100) UNIQUE,      -- Firebase UID
  display_name VARCHAR(100),
  character_id VARCHAR(20),
  gold INT, mp INT, hearts INT,
  cleared_levels JSONB,         -- 클리어한 레벨 ID 배열
  completed_gus JSONB,          -- 완료한 구 ID 배열
  completed_dongs JSONB,        -- 완료한 동 ID 배열
  inventory JSONB,              -- 보유 아이템
  boosters JSONB                -- 부스터 수량
)

-- 정적 데이터 (백업/동기화용)
regions, cities, districts, restaurants, levels, items
```

**API 연동 (`api-worker/src/index.js`):**
- `GET /api/user?uid=xxx` - 유저 데이터 조회
- `POST /api/user` - 유저 생성/업데이트
- `POST /api/sync` - 진행 상황 동기화 (병합)
- `GET /api/regions` - 지역 데이터 조회

---

#### 2. JSON 파일 기반 데이터베이스 (정적 게임 데이터)

> **역할**: 게임 콘텐츠 데이터 (오프라인 지원, 빠른 로딩)

**디렉토리 구조:**
```
data/
├── regions.json           # 17개 시/도 데이터
├── levels.json            # 레벨 설정 (목표점수, 이동수 등)
├── items.json             # 아이템 정보
├── cities/                # 시/군/구 데이터 (17개 파일)
│   ├── seoul.json         # 서울 25개 구
│   ├── busan.json         # 부산 16개 구/군
│   ├── incheon.json       # 인천 10개 구/군
│   └── ...
├── districts/             # 동 데이터 (43개 파일)
│   ├── seoul/             # 서울 25개 구 전체
│   ├── incheon/           # 인천 8개 구
│   └── busan/             # 부산 10개 구
└── restaurants/           # 맛집 데이터 (17개 파일)
```

**CSV 데이터 (백업/편집용):**
```
data/
├── CharacterData.csv      # 캐릭터 정보
├── ItemData.csv           # 아이템 정보
├── LevelData.csv          # 레벨 설정
├── RestaurantData.csv     # 맛집 정보
├── CityData.csv, GuData.csv, DongData.csv  # 지역 데이터
```

**로딩 방식 (`js/data-loader.js`):**
```javascript
// 비동기 JSON 로딩 (캐싱 지원)
const data = await DataLoader.load('data/cities/seoul.json');
```

---

#### 3. 데이터 흐름 비교

```
┌─────────────────────────────────────────────────────────────────┐
│                        데이터 저장소 구조                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────┐     ┌──────────────────────┐         │
│  │   JSON 파일           │     │   Neon PostgreSQL    │         │
│  │  (정적 게임 데이터)    │     │  (사용자 데이터)      │         │
│  ├──────────────────────┤     ├──────────────────────┤         │
│  │ • 지역/도시/동 데이터  │     │ • 사용자 프로필       │         │
│  │ • 레벨 설정           │     │ • 진행 상황          │         │
│  │ • 아이템 정보         │     │ • 인벤토리           │         │
│  │ • 맛집 데이터         │     │ • 클리어 기록        │         │
│  ├──────────────────────┤     ├──────────────────────┤         │
│  │ 특징:                 │     │ 특징:                │         │
│  │ ✓ 오프라인 사용 가능   │     │ ✓ 멀티 디바이스 동기화 │         │
│  │ ✓ CDN 캐싱            │     │ ✓ 충돌 해결 (병합)    │         │
│  │ ✓ 빠른 로딩           │     │ ✓ 백업/복원          │         │
│  └──────────────────────┘     └──────────────────────┘         │
│            │                            │                       │
│            ▼                            ▼                       │
│  ┌────────────────────────────────────────────────────────────┐│
│  │                    클라이언트 (Browser)                     ││
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        ││
│  │  │ DataLoader  │  │ CloudSync   │  │ LocalStorage│        ││
│  │  │ (JSON 로딩) │  │ (API 연동)  │  │ (오프라인)  │        ││
│  │  └─────────────┘  └─────────────┘  └─────────────┘        ││
│  └────────────────────────────────────────────────────────────┘│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Authentication
| 기술 | 용도 | 특징 |
|------|------|------|
| **Firebase Auth** | 사용자 인증 | Google OAuth, Email, Anonymous |

### Hosting & CDN
| 기술 | 용도 | URL |
|------|------|-----|
| **Firebase Hosting** | 정적 호스팅 | daedongmapgame.web.app |

### Development Tools
| 도구 | 용도 |
|------|------|
| **VS Code** | IDE |
| **Git** | 버전 관리 |
| **GitHub** | 코드 저장소 |
| **Python http.server** | 로컬 개발 서버 |

---

## 프로젝트 구조

```
daedongmapgame/
├── 📁 .agent/                    # AI Agent 워크플로우
│   └── workflows/
│       ├── deploy.md             # 배포 가이드
│       ├── dev-progress.md       # 개발 진행 상황
│       └── refactoring-plan.md   # 리팩토링 계획
│
├── 📁 api/                       # Cloudflare Workers API
│   ├── src/
│   │   └── index.ts              # API 엔드포인트
│   ├── wrangler.toml             # Cloudflare 설정
│   └── package.json
│
├── 📁 css/                       # 스타일시트
│   ├── style.css                 # 메인 스타일
│   └── mobile.css                # 모바일 최적화
│
├── 📁 data/                      # 게임 데이터 (JSON)
│   ├── regions.json              # 17개 시/도
│   ├── levels.json               # 레벨 정보
│   ├── items.json                # 아이템 정보
│   ├── 📁 cities/                # 시/군/구 데이터
│   │   ├── seoul.json            # 서울 25개 구
│   │   ├── incheon.json          # 인천 10개 구/군
│   │   ├── busan.json            # 부산 16개 구/군
│   │   └── ...                   # (17개 파일)
│   ├── 📁 districts/             # 동(洞) 데이터
│   │   ├── 📁 seoul/             # 서울 25개 구
│   │   ├── 📁 incheon/           # 인천 8개 구
│   │   └── 📁 busan/             # 부산 10개 구
│   └── 📁 restaurants/           # 맛집 데이터
│       └── ...                   # (17개 파일)
│
├── 📁 database/                  # 데이터베이스 스키마
│   ├── schema.sql                # 테이블 정의
│   └── seed-regions.sql          # 초기 데이터
│
├── 📁 docs/                      # 문서
│   └── ARCHITECTURE.md           # 아키텍처 문서 (현재 파일)
│
├── 📁 js/                        # JavaScript 모듈
│   ├── game.js                   # 게임 메인 로직
│   ├── puzzle.js                 # 퍼즐 엔진
│   ├── data-loader.js            # 비동기 데이터 로더
│   ├── dong-registry.js          # 동 데이터 레지스트리
│   ├── audio-manager.js          # 오디오 관리
│   ├── firebase-sync.js          # Firebase 인증 연동
│   ├── cloud-sync.js             # 클라우드 동기화
│   ├── region-data.js            # 지역 데이터
│   └── ...                       # (기타 모듈)
│
├── 📁 sounds/                    # 오디오 파일
│   ├── bgm/                      # 배경음악
│   └── sfx/                      # 효과음
│
├── 📁 assets/                    # 이미지 등 정적 자산
│   └── icons/                    # 앱 아이콘
│
├── index.html                    # 메인 HTML
├── manifest.json                 # PWA 매니페스트
├── firebase.json                 # Firebase 설정
└── .firebaserc                   # Firebase 프로젝트 설정
```

---

## 데이터 흐름

### 1. 게임 시작 흐름

```
┌──────────┐    ┌───────────┐    ┌──────────────┐    ┌───────────┐
│  User    │───►│  index    │───►│   Firebase   │───►│  Game.js  │
│  Access  │    │  .html    │    │    Auth      │    │   init()  │
└──────────┘    └───────────┘    └──────────────┘    └───────────┘
                                         │                  │
                                         ▼                  ▼
                                 ┌───────────────┐  ┌────────────────┐
                                 │ Cloud Sync    │  │  DataLoader    │
                                 │ Load Progress │  │ Load JSON Data │
                                 └───────────────┘  └────────────────┘
```

### 2. 지역 선택 흐름

```
┌──────────┐    ┌───────────┐    ┌────────────┐    ┌──────────────┐
│  전국    │───►│  시/도    │───►│  구/군     │───►│  동(洞)     │
│  지도    │    │  선택     │    │  선택      │    │  선택       │
└──────────┘    └───────────┘    └────────────┘    └──────────────┘
                                       │                  │
                                       ▼                  ▼
                               ┌─────────────────────────────────┐
                               │     DongDataRegistry            │
                               │  • 하드코딩 데이터 확인          │
                               │  • JSON 파일 동적 로드          │
                               │  • 캐시 관리                    │
                               └─────────────────────────────────┘
```

### 3. 진행 상황 동기화

```
┌──────────────┐         ┌────────────────┐         ┌──────────────┐
│   Client     │         │  Cloudflare    │         │    Neon      │
│  (Game.js)   │         │   Workers      │         │  PostgreSQL  │
└──────┬───────┘         └───────┬────────┘         └──────┬───────┘
       │                         │                         │
       │  1. Save Progress       │                         │
       │ ──────────────────────► │                         │
       │    (JWT + game_data)    │                         │
       │                         │  2. Validate & Store    │
       │                         │ ──────────────────────► │
       │                         │                         │
       │                         │  3. Confirm             │
       │                         │ ◄────────────────────── │
       │  4. Success Response    │                         │
       │ ◄────────────────────── │                         │
       │                         │                         │
```

---

## 배포 환경

### 환경 구분

| 환경 | URL | 용도 |
|------|-----|------|
| **Production** | daedongmapgame.web.app | 실 서비스 |
| **Local** | localhost:8000 | 개발/테스트 |

### 배포 방법

#### 1. 로컬 개발 서버
```bash
cd d:\DEVELOP\WORKSPACE\daedongmapgame
python -m http.server 8000
# 접속: http://localhost:8000
```

#### 2. Firebase Hosting 배포
```bash
# Firebase CLI 로그인 (최초 1회)
npx firebase-tools login

# 배포
npx firebase-tools deploy

# 특정 기능만 배포
npx firebase-tools deploy --only hosting
```

#### 3. Cloudflare Workers API 배포
```bash
cd api
npm install
npx wrangler deploy
```

### Firebase 설정 (`firebase.json`)
```json
{
  "hosting": {
    "public": ".",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**",
      "api/**",
      "database/**",
      "docs/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ],
    "headers": [
      {
        "source": "**/*.@(js|css|json)",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "max-age=31536000"
          }
        ]
      }
    ]
  }
}
```

---

## CI/CD 파이프라인

### 현재 상태: 수동 배포

현재는 수동 배포 방식을 사용 중입니다:

```
┌────────────┐    ┌────────────┐    ┌────────────┐    ┌────────────┐
│   Local    │───►│   GitHub   │───►│   Manual   │───►│  Firebase  │
│  Changes   │    │   Push     │    │  Deploy    │    │  Hosting   │
└────────────┘    └────────────┘    └────────────┘    └────────────┘
```

### 권장 CI/CD 구성 (GitHub Actions)

`.github/workflows/deploy.yml`:

```yaml
name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install Firebase CLI
        run: npm install -g firebase-tools
      
      - name: Deploy to Firebase
        run: firebase deploy --only hosting
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
      
      # API 배포 (선택)
      - name: Deploy Cloudflare Workers
        if: contains(github.event.head_commit.message, '[api]')
        working-directory: ./api
        run: |
          npm install
          npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
```

### GitHub Secrets 설정 필요

| Secret Name | 설명 | 획득 방법 |
|-------------|------|----------|
| `FIREBASE_TOKEN` | Firebase CI 토큰 | `firebase login:ci` |
| `CLOUDFLARE_API_TOKEN` | Cloudflare API 토큰 | Cloudflare 대시보드 |

### CI/CD 구현 단계

1. **GitHub Repository 설정**
   ```bash
   git remote add origin https://github.com/CHOIHYUKMIN/DaedongMapGame.git
   git push -u origin main
   ```

2. **Firebase Token 생성**
   ```bash
   firebase login:ci
   # 출력된 토큰을 GitHub Secrets에 저장
   ```

3. **Workflow 파일 생성**
   ```bash
   mkdir -p .github/workflows
   # deploy.yml 파일 생성
   ```

---

## 보안 구성

### 인증 흐름

```
┌─────────┐    ┌──────────────┐    ┌────────────────┐    ┌──────────┐
│  User   │───►│   Firebase   │───►│   JWT Token    │───►│   API    │
│  Login  │    │   Auth       │    │   (id_token)   │    │  Request │
└─────────┘    └──────────────┘    └────────────────┘    └──────────┘
                                           │
                                           ▼
                                   ┌───────────────────┐
                                   │  Cloudflare Worker │
                                   │  Token Validation  │
                                   └───────────────────┘
```

### API 보안

1. **JWT 토큰 검증**: 모든 API 요청에 Firebase ID Token 필수
2. **CORS 설정**: 허용된 도메인만 API 접근 가능
3. **Rate Limiting**: Cloudflare 기본 제공 (DDoS 방어)
4. **HTTPS**: 모든 통신 암호화

### 환경 변수 관리

```bash
# Cloudflare Workers Secrets
wrangler secret put DATABASE_URL
wrangler secret put FIREBASE_PROJECT_ID
```

---

## 모니터링 및 로깅

### 현재 구현된 모니터링

1. **Console 로깅**: 개발 중 디버깅용
   ```javascript
   console.log('📍 구 선택: ${guId}');
   console.log('✅ 데이터 로드 완료');
   console.error('❌ 오류 발생');
   ```

2. **Firebase Analytics**: 기본 사용자 행동 추적

### 권장 모니터링 도구

| 도구 | 용도 | 비용 |
|------|------|------|
| **Cloudflare Analytics** | API 트래픽 모니터링 | 무료 |
| **Firebase Performance** | 앱 성능 측정 | 무료 티어 |
| **Sentry** | 에러 트래킹 | 무료 티어 |

---

## 확장 계획

### 단기 (1-2주)
- [ ] 전국 동 데이터 완성
- [ ] 성능 최적화 (Lazy Loading)
- [ ] 오프라인 지원 강화

### 중기 (1개월)
- [ ] GitHub Actions CI/CD 구축
- [ ] 자동 테스트 추가
- [ ] 에러 모니터링 연동

### 장기 (3개월+)
- [ ] 랭킹 시스템
- [ ] 소셜 기능 (친구, 공유)
- [ ] 다국어 지원

---

*이 문서는 프로젝트 변경에 따라 지속적으로 업데이트됩니다.*
